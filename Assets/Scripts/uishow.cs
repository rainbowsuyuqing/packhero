using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class uishow : MonoBehaviour

{
    // Start is called before the first frame update
    void Start()
    {
        //bool show1=GameObject.Find("GameController").GetComponent<TokenController>().showpopup1;
         //bool show1=GameObject.Find("Player").GetComponent<PlayerController>().showpopup1;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
